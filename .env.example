## App (Vite / TanStack Start)
# Provided by Clerk dashboard
VITE_CLERK_PUBLISHABLE_KEY=

# Optional: name of the Clerk JWT template used for Convex
VITE_CLERK_JWT_TEMPLATE=convex

# Provided by `npx convex dev`
VITE_CONVEX_URL=

# Optional: used when generating share links (otherwise falls back to window.origin)
VITE_SITE_URL=http://localhost:3000

## Convex (set in Convex dashboard / `npx convex env set`)
# Issuer domain for Clerk JWTs (from your Clerk JWT template settings)
CLERK_JWT_ISSUER_DOMAIN=

